import SubmitPage from "./pages/SubmitPage";

// Phase 2+ will introduce react-router-dom routes here for the
// authenticated MP views (/map, /priorities, /compare). Phase 1 is a
// single public route, so a router is deliberately left out for now.
function App() {
  return <SubmitPage />;
}

export default App;
